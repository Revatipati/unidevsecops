apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: chrp-tomcat
  name: chrplb1
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: chrp-tomcat
  type: LoadBalancer
status:
  loadBalancer: {}
